# Project Conventions

## YAML file naming

All YAML files use the `.yaml` extension:

- `.github/workflows/*.yaml` — GitHub Actions workflows
- `lefthook.yaml` — Pre-commit hooks config

**Exception:** `.github/dependabot.yml` remains `.yml` because GitHub Dependabot requires that exact filename.
